/*
package razorthink.dao;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;
import razorthink.models.UserAccountMapping;

import javax.transaction.Transactional;

*/
/**
 * Created by sethulakshmi on 26/4/17.
 *//*


@Repository
@Transactional
public class UserAccountMappingDao
{
    @Autowired
    private SessionFactory sessionFactory;

    private Session getSession()
    {
        return sessionFactory.getCurrentSession();
    }

    public void save(UserAccountMapping userAccountMapping)
    {
        getSession().save(userAccountMapping);
        return;
    }
}
*/
